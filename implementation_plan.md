# 持久化账号与掉线重连系统设计方案

为了彻底解决刷新掉线、服务器重启导致筹码丢失的问题，本次更新将结合**“服务器本地账号持久化”**与**“短时掉线保护”**两个机制。只需输入 ID 即可登录，同时保证游戏连贯性。

## User Review Required

> [!IMPORTANT]
> 1. **免密持久化登录**：按照你的要求，只要输入对应 ID 即可登录（无需密码）。后台会建立一个 `users.json` 文件来永久保存每个 ID 的全局筹码等信息。即使服务器重启，玩家的筹码也不会丢失。
> 2. **账号防盗用**：为了防止别人恶意使用你的 ID 登录，目前的规则是：**如果该 ID 正在线上，其他人无法登录**。如果你意外断线，ID 会在 1 分钟内被锁定，**只有你当前浏览器的缓存**或**你在1分钟内再次输入该ID**可以重新连入游戏。
> 3. **前端无感自动重连**：登录过的 ID 会记录在浏览器的 `localStorage` 中。当你刷新网页时，前端会自动静默登录，并直接带你回到刚才的牌桌坐下，**一切都在瞬间完成，不会干扰游戏节奏**。
> 4. **退出机制**：由于加入了自动登录，网页上会新增一个【切换账号/退出登录】按钮，方便你更换 ID。在牌桌内如果主动点击【返回大厅】，会立即离开牌桌并结算筹码。

## Proposed Changes

### 1. 账号持久化 (Backend)
#### [NEW] `backend/data/users.json` (自动生成)
- 记录所有注册过的玩家 ID、全局筹码、最后登录时间等。
#### [MODIFY] `backend/main.py`
- 增加读取和保存 `users.json` 的逻辑，替代原有的内存字典 `user_global_chips`。
- 修改 `/api/login`：查询或创建持久化账号，返回对应的筹码。

### 2. 牌桌重连机制 (Backend)
#### [MODIFY] `backend/poker_logic/game_state.py`
- 为 `Player` 模型增加 `is_online: bool = True` 字段。
#### [MODIFY] `backend/main.py`
- `/ws/{table_id}/{client_id}`：
  - 收到明确的 `{"action": "leave"}` 时，立即结算并移出牌桌。
  - 直接断开 WS 连接（如刷新网页）时，将其 `is_online` 设为 `False` 并保留座位，启动 **60秒** 倒计时任务。
  - 若 60 秒内同一 ID 重新连接，则取消倒计时，恢复 `is_online = True`，继续原有的手牌和筹码打牌。
  - 若超时未重连，则强制弃牌并移出座位，将赚取的筹码更新到 `users.json`。

### 3. 前端持久化与展示 (Frontend)
#### [MODIFY] `frontend/src/App.tsx`
- 登录成功时，将用户名保存到 `localStorage`。
- 页面初始加载时，如果有缓存的 ID，自动静默调用 `/api/login` 并跳过登录页面。
- 在大厅界面增加【退出登录】按钮，清除缓存以便更换 ID。
#### [MODIFY] `frontend/src/components/PokerTable.tsx`
- 将原本的卸载时直接离桌改为：点击“返回大厅”时主动发送 `"leave"` 指令。
- 牌桌上根据其他玩家的 `is_online` 状态，在他们头像/名字旁显示 `[🔴 掉线中]` 的特殊标识，并且头像置灰。

## Verification Plan

### Manual Verification
1. **持久化测试**：登录分配一个新 ID，在大厅看到 5 个筹码。重启后端服务器。再次登录该 ID，检查筹码是否依然保留。
2. **断线保护测试**：打牌中途强制刷新网页（或按 F5）。页面应瞬间自动恢复，且牌桌座位、手牌、底池依然是断线前的状态，没有任何损失。
3. **掉线标识测试**：双开浏览器进入同一牌桌，强退其中一个。另一个浏览器上应立刻看到对方名字旁出现“掉线中”提示。1 分钟后，掉线者自动弃牌并消失。
